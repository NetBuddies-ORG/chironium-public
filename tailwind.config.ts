// Tailwind CSS v4 configuration
// Force class-based dark mode so the <html class="dark"> toggle from ThemeProvider works regardless of OS preference.
export default {
  darkMode: 'class',
} as const;
